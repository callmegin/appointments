import { FlexDiv } from 'shared/styles';
import styled from 'styled-components';

export const Slots = styled(FlexDiv)`
  height: 100%;
`;

export const ButtonBottom = styled.div`
  margin-top: auto;
`;

export const ButtonWrapper = styled.div`
  margin-top: 1.5rem;
`;
