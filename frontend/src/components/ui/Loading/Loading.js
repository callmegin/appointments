import * as s from './styled';

const Loading = () => {
  return (
    <s.LoadingWrapper column alignCenter>
      <s.LoadingCube />
    </s.LoadingWrapper>
  );
};

export default Loading;
